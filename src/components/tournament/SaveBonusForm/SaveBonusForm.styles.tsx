import styled from "styled-components";

import { SaveBonusFormStyledProps as Props } from "./SaveBonusForm.types";

const SaveBonusFormStyled = styled.div<Props>`
  .SaveBonusForm {
  }
`;

export default SaveBonusFormStyled;
